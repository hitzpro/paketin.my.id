---
// src/components/payment/QRCodeDisplay.astro
interface Props {
  checkoutId: string;
  price: string;
}
import { API_BASE_URL } from '../../config'; // Butuh URL frontend sebenernya

const { checkoutId, price } = Astro.props;


const scanUrl = `${Astro.url.origin}/pay-confirm/${checkoutId}`; 
const qrUrl = `https://api.qrserver.com/v1/create-qr-code/?size=250x250&data=${encodeURIComponent(scanUrl)}`;
---

<img src={qrUrl} ... />