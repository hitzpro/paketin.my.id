---
// src/components/profile/ProfileFormImage.astro
---
<div class="flex flex-col items-center justify-center mb-6">
    <div class="relative group">
        <img 
            id="profile-image-preview" 
            class="w-32 h-32 rounded-full object-cover border-4 border-white dark:border-gray-700 shadow-lg" 
            src="https://ui-avatars.com/api/?name=User&background=random" 
            alt="Foto Profil"
        >
        
        <label 
            for="profile-image-input" 
            id="profile-image-label" 
            class="hidden absolute bottom-0 right-0 bg-blue-600 text-white p-2 rounded-full cursor-pointer hover:bg-blue-700 transition-colors shadow-md"
        >
            <i class="fa-solid fa-camera"></i>
        </label>
        
        <input 
            type="file" 
            id="profile-image-input" 
            class="hidden" 
            accept="image/png, image/jpeg, image/jpg" 
            disabled
        >
    </div>
    <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">Klik tombol "Ubah Profil" untuk mengganti foto.</p>
    
    <canvas id="image-canvas" class="hidden"></canvas>
</div>